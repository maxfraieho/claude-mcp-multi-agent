# 🔑 Як отримати робочі Gemini API токени

## Проблема

Поточні токени мають **"limit: 0"**, що означає:
- Вони не активовані для Gemini API
- Або це demo/example токени з документації
- Або акаунти не мають доступу до Gemini free tier

## ✅ Правильний спосіб отримати токени

### Крок 1: Відкрити Google AI Studio
Перейдіть на: https://aistudio.google.com/apikey

### Крок 2: Увійти в Google акаунт
- Використовуйте Gmail акаунт
- Якщо немає акаунту - створіть новий

### Крок 3: Створити новий API ключ
1. Натисніть **"Create API key"** або **"Get API key"**
2. Виберіть існуючий Google Cloud проект або створіть новий
3. Скопіюйте згенерований ключ (формат: `AIzaSy...`)

### Крок 4: Перевірити доступ до Gemini 2.0
- Переконайтесь що у вас є доступ до **Gemini 2.0 Flash**
- В AI Studio має бути доступна модель для тестування
- Free tier: 15 requests/minute, 1500 requests/day

### Крок 5: Протестувати ключ
```bash
# Тест через curl
curl "https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash-exp:generateContent?key=YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "contents": [{
      "parts": [{
        "text": "Test"
      }]
    }]
  }'
```

Якщо отримаєте відповідь (не 429 з limit:0) - ключ працює!

### Крок 6: Додати токени в систему

**Опція A: Вручну**
```bash
# Створіть файл з токенами
cat > /home/vokov/multi-agent-dev-system/agent-dev/secrets/gemini_tokens.txt << 'EOF'
AIzaSy_ВАШ_ПЕРШИЙ_КЛЮЧ
AIzaSy_ВАШ_ДРУГИЙ_КЛЮЧ
AIzaSy_ВАШ_ТРЕТІЙ_КЛЮЧ
EOF

# Перезапустіть проксі
docker restart gemini-proxy
```

**Опція B: Через Docker**
```bash
# Скопіюйте в контейнер
docker cp /path/to/gemini_tokens.txt gemini-proxy:/app/secrets/gemini_tokens.txt
docker restart gemini-proxy
```

## 🔍 Перевірка після додавання

### 1. Перевірити чи токени завантажені
```bash
docker logs --tail 20 gemini-proxy | grep "Завантажено"
# Має бути: ✅ Завантажено N токенів
```

### 2. Перевірити health
```bash
curl http://localhost:8080/health | jq
# "active_tokens": N  ← має бути > 0
```

### 3. Тестовий запит
```bash
curl -X POST http://localhost:8080/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gemini-2.0-flash-exp",
    "messages": [{"role": "user", "content": "Test"}],
    "max_tokens": 10
  }'
```

**Очікуваний результат:**
- ✅ Status 200
- ✅ Відповідь у JSON форматі з choices
- ❌ НЕ 429 з "limit: 0"

## 📝 Рекомендації

### Для безкоштовного використання:
- Створіть 3-6 різних Google акаунтів
- Отримайте API ключ з кожного
- Додайте всі ключі в `gemini_tokens.txt` (по одному на рядок)
- Proxy автоматично ротуватиме між ними

### Free tier ліміти:
```
Per API key:
- 15 requests per minute
- 1,500 requests per day
- 1,000,000 tokens per day

При 6 ключах:
- 90 requests per minute (1.5 req/sec)
- 9,000 requests per day
```

### Для production:
- Розгляньте paid tier
- Більші ліміти
- Кращий SLA
- Priority support

## ⚠️ Важливо

**НЕ використовуйте:**
- Demo/example ключі з документації
- Ключі з GitHub / публічних репозиторіїв
- Чужі ключі без дозволу
- Expired або деактивовані ключі

**ЗАВЖДИ:**
- Тримайте ключі в секреті
- Не комітьте в git (додано в .gitignore)
- Регулярно перевіряйте usage на https://ai.dev/usage
- Видаляйте невикористані ключі

## 🔗 Корисні посилання

- Google AI Studio: https://aistudio.google.com
- API Keys: https://aistudio.google.com/apikey
- Documentation: https://ai.google.dev/gemini-api/docs
- Rate Limits: https://ai.google.dev/gemini-api/docs/rate-limits
- Usage Monitor: https://ai.dev/usage

---

**Створено:** Claude Code
**Дата:** 2025-11-15
