# 🎯 Налаштування Vibe Kanban для роботи з Multi-Agent System через MCP

Вітаю! 👋 Ця інструкція допоможе вам налаштувати та використовувати Vibe Kanban для роботи з багатоагентною системою розробки через Model Context Protocol (MCP).

## 🌟 Що ви отримаєте

Після налаштування ви зможете:
- ✅ Створювати задачі в зручному Kanban інтерфейсі
- ✅ Автоматично делегувати задачі найкращому AI агенту (Claude, Gemini, Qwen)
- ✅ Відстежувати прогрес виконання в реальному часі
- ✅ Використовувати оркестрацію між агентами для складних задач
- ✅ Працювати з кодом через git worktrees (ізольовані робочі середовища)

---

## 📋 Передумови

Перевірте що у вас запущені сервіси:

```bash
# Перевірка всіх контейнерів
docker ps

# Повинні бути запущені:
# - vibe-kanban (port 3001)
# - gemini-proxy (port 8080)
# - postgres (port 5432)
# - redis (port 6379)
# - vault (port 8200)
```

**Очікуваний вивід:**
```
vibe-kanban      Up X hours (healthy)   0.0.0.0:3001->3000/tcp
gemini-proxy     Up X hours (healthy)   0.0.0.0:8080->8080/tcp
postgres         Up X hours (healthy)   0.0.0.0:5432->5432/tcp
redis            Up X hours (healthy)   0.0.0.0:6379->6379/tcp
vault            Up X hours (unhealthy) 0.0.0.0:8200->8200/tcp
```

---

## 🔧 Крок 1: Перевірка MCP конфігурації

### 1.1 Перевірте наявність .mcp.json

```bash
# Перейдіть в директорію Vibe Kanban
cd /home/vokov/multi-agent-dev-system/agent-dev/vibe-kanban

# Перевірте конфігурацію
cat .mcp.json | head -20
```

**Що ви побачите:**
```json
{
  "mcpServers": {
    "multi-agent-system": {
      "command": "node",
      "args": ["/app/mcp-server/index.js"],
      ...
    }
  },
  "executors": {
    "claude-code-orchestrator": { ... },
    "gemini-flash-2.0-proxy": { ... }
  }
}
```

### 1.2 Перевірте доступність MCP сервера

```bash
# З хоста (якщо MCP сервер запущений окремо)
curl -s http://localhost:8765/health || echo "MCP server не доступний"

# Або перевірте з Docker мережі
docker exec vibe-kanban curl -s http://mcp-server:8765/health
```

---

## 🚀 Крок 2: Доступ до Vibe Kanban

### 2.1 Відкрийте веб-інтерфейс

Відкрийте браузер та перейдіть до:
```
http://localhost:3001
```

### 2.2 Перша авторизація (GitHub OAuth)

Vibe Kanban використовує GitHub для авторизації:

1. При першому вході побачите екран авторизації
2. Натисніть "Sign in with GitHub"
3. Виконайте Device Flow:
   - Скопіюйте код який показано
   - Перейдіть на https://github.com/login/device
   - Введіть код і підтвердіть доступ

**Примітка:** Vibe Kanban використовує публічний GitHub App від Bloop AI для розробки. Для production варто створити власний GitHub App.

---

## 📝 Крок 3: Створення проекту та задач

### 3.1 Створення нового проекту

1. Натисніть **"New Project"** в інтерфейсі
2. Введіть:
   - **Назва проекту:** "Multi-Agent Development"
   - **Опис:** "Розробка з використанням Claude, Gemini та Qwen"
   - **Git Repository:** Шлях до вашого git репозиторію (опціонально)

### 3.2 Створення задачі

1. В проекті натисніть **"New Task"**
2. Заповніть:
   - **Заголовок:** Короткий опис задачі
   - **Опис:** Детальний опис що потрібно зробити
   - **Executor:** Виберіть AI агента для виконання

**Приклад задачі:**
```
Заголовок: Додати валідацію email в форму реєстрації
Опис:
- Додати regex валідацію email
- Показувати помилку якщо email невалідний
- Додати тести для валідації

Executor: gemini-flash-2.0-proxy
```

---

## 🤖 Крок 4: Вибір правильного агента

### 4.1 Доступні Executors

#### 🎭 **claude-code-orchestrator** (Рекомендовано!)
**Використовуй для:**
- Складних multi-step задач
- Коли потрібен інтелектуальний вибір найкращого агента
- Архітектурних рішень
- Планування та аналізу

**Приклад:**
```
"Створи REST API для управління користувачами з автентифікацією"
```

#### ⚡ **gemini-flash-2.0-proxy**
**Використовуй для:**
- Швидкого рефакторингу коду
- Написання тестів
- Аналізу коду
- Генерації документації

**Приклад:**
```
"Додай TypeScript типи до всіх функцій в user.service.ts"
```

#### 🧠 **gemini-flash-thinking-proxy**
**Використовуй для:**
- Складних алгоритмічних задач
- Debugging складних проблем
- Архітектурних рішень
- Problem-solving з reasoning

**Приклад:**
```
"Оптимізуй алгоритм пошуку для великих датасетів"
```

#### 📚 **gemini-pro-proxy**
**Використовуй для:**
- Роботи з великим контекстом (32K tokens)
- Складної документації
- Code review великих файлів
- Рефакторингу великих модулів

**Приклад:**
```
"Проаналізуй всю кодову базу і знайди potential memory leaks"
```

#### 💻 **qwen-2.5-coder-ssh**
**Використовуй для:**
- Спеціалізованого coding
- Debugging на віддаленому сервері
- Робоча з специфічними мовами програмування

**Примітка:** Потребує SSH доступу до віддаленого сервера (100.100.74.9)

---

## 🎬 Крок 5: Запуск та моніторинг задачі

### 5.1 Запуск виконання

1. Знайдіть створену задачу в проекті
2. Натисніть **"Start"** або **"Run Task"**
3. Vibe Kanban автоматично:
   - Створить git worktree (ізольоване середовище)
   - Запустить обраного AI агента
   - Почне стрімити логи в реальному часі

### 5.2 Моніторинг прогресу

**Перегляд логів в реальному часі:**
```
http://localhost:3001/projects/{project_id}/tasks/{task_id}
```

Ви побачите:
- 📋 Логи виконання агента
- 🔄 Diff змін в коді (якщо агент редагує файли)
- ✅ Статус виконання (running, completed, failed)

**Через API (якщо потрібно):**
```bash
# Отримати логи задачі
curl http://localhost:3001/api/events/task-attempts/{attempt_id}/logs

# Отримати diff змін
curl http://localhost:3001/api/events/task-attempts/{attempt_id}/diff
```

### 5.3 Статуси задачі

- **🟡 Pending:** Задача створена, очікує виконання
- **🔵 Running:** Агент виконує задачу
- **🟢 Completed:** Задача успішно виконана
- **🔴 Failed:** Помилка виконання (дивіться логи)

---

## 🎼 Крок 6: Оркестрація між агентами

### 6.1 Як працює Claude Orchestrator

Коли ви обираєте **claude-code-orchestrator**, він:

1. **Аналізує задачу** і визначає її складність
2. **Обирає найкращого агента:**
   - Gemini Flash → для простих задач
   - Gemini Thinking → для складного reasoning
   - Qwen → для спеціалізованого coding
3. **Може делегувати підзадачі** різним агентам
4. **Агрегує результати** від усіх агентів

**Приклад складної задачі:**
```
Задача: "Створи мікросервіс для обробки платежів"

Claude Orchestrator:
1. Gemini Thinking → Проектує архітектуру
2. Gemini Flash → Генерує boilerplate код
3. Qwen → Пише складну бізнес-логіку
4. Gemini Pro → Пише документацію і тести
```

### 6.2 Fallback механізм

Якщо обраний агент недоступний, система автоматично:
- Використає fallback агента (вказаного в .mcp.json)
- Або перенаправить до Claude Orchestrator для вибору альтернативи

---

## 🔍 Крок 7: Робота з результатами

### 7.1 Перегляд змін у коді

Після виконання задачі:

1. Відкрийте задачу в Vibe Kanban
2. Перейдіть на вкладку **"Diff"**
3. Побачите всі зміни які зробив агент

### 7.2 Застосування змін

```bash
# Vibe Kanban автоматично створює git worktree
# Знайдіть worktree задачі:
cd /path/to/your/project
git worktree list

# Перегляньте зміни
cd .worktrees/task-{id}
git diff

# Якщо зміни влаштовують - merge в main branch
git checkout main
git merge task-{id}-branch
```

### 7.3 Очищення worktrees

Vibe Kanban автоматично очищує старі worktrees, але можна зробити вручну:

```bash
# Видалити конкретний worktree
git worktree remove .worktrees/task-123

# Очистити всі orphaned worktrees
git worktree prune
```

---

## 🛠️ Крок 8: Troubleshooting

### ❌ Проблема: "MCP server не доступний"

**Причина:** MCP сервер не запущений або недоступний з Docker мережі

**Рішення:**
```bash
# Перевірте чи запущений MCP контейнер
docker ps | grep mcp

# Якщо немає - запустіть через docker-compose
cd /home/vokov/multi-agent-dev-system/agent-dev
docker-compose up -d mcp-server

# Перевірте логи
docker logs mcp-server
```

---

### ❌ Проблема: "Gemini proxy не відповідає"

**Причина:** Gemini proxy не запущений або закінчились API токени

**Рішення:**
```bash
# Перевірте статус
curl http://localhost:8080/health

# Повинно повернути:
# {"status":"healthy","version":"2.0.0",...}

# Перевірте логи
docker logs gemini-proxy

# Перевірте токени
ls -la /home/vokov/multi-agent-dev-system/agent-dev/secrets/gemini_token_*.txt
```

---

### ❌ Проблема: "Executor not found"

**Причина:** .mcp.json не синхронізовано з Vibe Kanban

**Рішення:**
```bash
# Перезапустіть Vibe Kanban для перечитування конфігу
docker restart vibe-kanban

# Або скопіюйте .mcp.json знову
cp /home/vokov/multi-agent-dev-system/agent-dev/vibe-kanban-mcp-config.json \
   /home/vokov/multi-agent-dev-system/agent-dev/vibe-kanban/.mcp.json

docker restart vibe-kanban
```

---

### ❌ Проблема: "SSH connection failed" (для Qwen)

**Причина:** Немає SSH доступу до віддаленого сервера

**Рішення:**
```bash
# Перевірте SSH підключення
ssh vokov@100.100.74.9

# Якщо не працює - використовуйте Gemini/Claude замість Qwen
# Або налаштуйте SSH ключі:
ssh-copy-id vokov@100.100.74.9
```

---

### ❌ Проблема: "Task застрягла в Running"

**Причина:** Агент завис або втратив з'єднання

**Рішення:**
```bash
# Знайдіть process ID задачі в логах Vibe Kanban
docker logs vibe-kanban | grep "task-{id}"

# Перезапустіть задачу через UI
# Або вручну очистіть worktree і створіть нову задачу
```

---

## 📊 Крок 9: Моніторинг системи

### 9.1 Grafana Dashboard

Відкрийте http://localhost:3000 (admin/admin)

Доступні метрики:
- Використання агентів (requests/sec)
- Час виконання задач
- Success/Failure rate
- API токени ротація (Gemini)

### 9.2 Prometheus Metrics

Прямий доступ до метрик:
```bash
# Gemini Proxy metrics
curl http://localhost:8080/metrics

# Vibe Kanban metrics (якщо expo зовані)
curl http://localhost:3001/metrics
```

---

## 🎯 Приклади Use Cases

### Use Case 1: Рефакторинг коду

**Задача:** "Рефакторинг user.service.ts - розділити на окремі модулі"

**Executor:** `gemini-flash-2.0-proxy`

**Очікуваний результат:**
- Код розділено на user.service.ts, user.repository.ts, user.validator.ts
- Тести оновлені
- Diff показує всі зміни

---

### Use Case 2: Складна архітектурна задача

**Задача:** "Спроектувати мікросервісну архітектуру для e-commerce"

**Executor:** `claude-code-orchestrator`

**Що відбувається:**
1. Claude аналізує requirements
2. Делегує до Gemini Thinking → Створює архітектурну діаграму
3. Делегує до Gemini Flash → Генерує boilerplate services
4. Повертає комплексне рішення

---

### Use Case 3: Debugging production issue

**Задача:** "Знайти причину memory leak в payment service"

**Executor:** `gemini-flash-thinking-proxy`

**Процес:**
1. Аналіз коду з reasoning
2. Виявлення потенційних leaks
3. Пропозиції щодо виправлення
4. Модифікований код з fixes

---

## 📚 Додаткові ресурси

### Документація

- **Vibe Kanban:** https://github.com/BloopAI/vibe-kanban
- **MCP Protocol:** https://github.com/anthropics/mcp
- **Gemini API:** https://ai.google.dev/docs
- **Проект GitHub:** https://github.com/maxfraieho/claude-mcp-multi-agent

### Конфігураційні файли

- `.mcp.json` - MCP сервери та executors
- `docker-compose-production.yml` - Production infrastructure
- `config.yaml` - Системна конфігурація
- `vibe-kanban-mcp-config.json` - Template конфігурації

---

## 🎉 Готово!

Тепер ви готові використовувати Vibe Kanban для керування AI-powered розробкою!

**Швидкий старт:**
1. Відкрийте http://localhost:3001
2. Створіть новий проект
3. Додайте задачу з описом що потрібно зробити
4. Виберіть executor (рекомендуємо `claude-code-orchestrator`)
5. Натисніть Start і спостерігайте за магією! ✨

**Питання або проблеми?**
- Перевірте розділ Troubleshooting вище
- Подивіться логи: `docker logs vibe-kanban`
- Перевірте GitHub Issues: https://github.com/maxfraieho/claude-mcp-multi-agent/issues

---

**Створено з ❤️ за допомогою Claude Code**
